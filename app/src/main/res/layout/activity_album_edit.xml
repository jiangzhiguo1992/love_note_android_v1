<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    tools:context="com.jiangzg.lovenote.activity.note.AlbumEditActivity">

    <include layout="@layout/view_top_bar"/>

    <EditText
        android:id="@+id/etTitle"
        style="@style/FontBlackNormal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/abl"
        android:layout_marginStart="50dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="50dp"
        android:layout_marginBottom="30dp"
        android:background="@drawable/shape_stroke_grey_r3"
        android:hint="@string/please_input_album_name"
        android:maxLines="1"
        android:padding="10dp"
        android:singleLine="true"
        android:textColorHint="@color/font_hint"/>

    <ImageView
        android:id="@+id/ivAdd"
        android:layout_width="match_parent"
        android:layout_height="270dp"
        android:layout_below="@+id/etTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="50dp"
        android:layout_marginEnd="50dp"
        android:background="@drawable/shape_stroke_grey_r3"
        android:elevation="@dimen/elevation_normal"
        android:scaleType="fitCenter"
        android:src="@mipmap/ic_add_grey_24dp"
        android:visibility="gone"
        tools:visibility="visible"/>

    <com.jiangzg.lovenote.view.FrescoView
        android:id="@+id/ivAlbum"
        android:layout_width="match_parent"
        android:layout_height="270dp"
        android:layout_below="@+id/etTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="50dp"
        android:layout_marginEnd="50dp"
        android:background="?attr/selectableItemBackground"
        android:foreground="?attr/selectableItemBackground"
        android:visibility="gone"
        app:actualImageScaleType="centerCrop"
        tools:scaleType="centerCrop"
        tools:src="@mipmap/ic_launcher"
        tools:visibility="gone"/>
</RelativeLayout>
