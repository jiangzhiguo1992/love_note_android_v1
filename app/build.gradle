apply plugin: 'com.android.application'

android {
    buildToolsVersion '26.0.3'
    compileSdkVersion 27 // 最高运行版本
    defaultConfig {
        targetSdkVersion 27 // 最高效果版本
        minSdkVersion 21 // 最低运行版本
        multiDexEnabled true // 允许分包
        applicationId "com.jiangzg.ita" // 要和manifest一致
        versionCode 1
        versionName "1.0.0"
        vectorDrawables.useSupportLibrary = true // 用于支持vectorDrawables
        flavorDimensions "release" // 多渠道打包分支
    }
    signingConfigs { // 签名信息
        jzg_01 {
            storeFile file('/Users/gg/Documents/mine/android/key/jzg_01')
            storePassword 'laojiang1'
            keyAlias 'jzg_01'
            keyPassword 'laojiang1'
        }
    }
    buildTypes { // 打包选项
        debug { // 测试
            signingConfig signingConfigs.jzg_01 // 设置签名信息
            minifyEnabled false // 混淆开关(测试最好关掉，否则会影响断点调试)
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            // 混淆规则文件
        }
        release { // 正式
            signingConfig signingConfigs.jzg_01 // 可以只用一个签名信息
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        applicationVariants.all { variant ->
            variant.outputs.all { output ->
                def outputFile = output.outputFile
                if (outputFile != null && outputFile.name.endsWith('.apk')) {
                    // 输出apk名称为app_code_date.apk
                    def date = new Date().format("yyyy-MM-dd-HH-mm")
                    outputFileName = "ita_${android.defaultConfig.versionCode}_${variant.productFlavors[0].name}_${date}.apk"
                }
            }
        }
    }
    productFlavors { // 发布渠道
        //---------------------手机市场----------------------
        sanxing { // 三星应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "sanxing"]
        }
        xiaomi { // 小米应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "xiaomi"]
        }
        huawei { // 华为应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "huawei"]
        }
        vivo { // vivo应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "vivo"]
        }
        oppo { // oppo应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "oppo"]
        }
        meizu { // 魅族应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "meizu"]
        }
        nubiya { // 努比亚应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "nubiya"]
        }
        yijia { // 一加应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "yijia"]
        }
        jinli { // 金立应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "jinli"]
        }
        nuojiya { // 诺基亚应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "nuojiya"]
        }
        //---------------------附属市场----------------------
        yingyongbao { // 应用宝
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "yingyongbao"]
        }
        qh360 { // 360手机助手
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "qh360"]
        }
        baidu { // 百度助手
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "baidu"]
        }
        taobao { // 淘宝手机助手
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "taobao"]
        }
        sougou { // 搜狗应用市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "sougou"]
        }
        wo { // 沃商店
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "wo"]
        }
        //---------------------专职市场----------------------
        wandoujia { // 豌豆荚
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "wandoujia"]
        }
        ku { // 酷市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "ku"]
        }
        yingyonghui { // 应用汇
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "yingyonghui"]
        }
        mumayi { // 木蚂蚁
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "mumayi"]
        }
        nineone { // 91手机助手
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "nineone"]
        }
        anzhi { // 安智市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "anzhi"]
        }
        anzhuo { // 安卓市场
            dimension "release"
            manifestPlaceholders = [UMENG_CHANNEL_VALUE: "anzhuo"]
        }
    }
    sourceSets { // 源码设置
        main {
            java.srcDirs = ['src/main/java', 'src/main/aidl']
            jniLibs.srcDirs = ['libs'] // 指定lib库目录
        }
        instrumentTest.setRoot('tests') // 指定测试所在的路径
        debug.setRoot('build-types/debug') // 指定debug模式的路径
        release.setRoot('build-types/release') // 指定release模式的路径
    }
    compileOptions { // 编码/JDK等
        encoding "UTF-8"
//        sourceCompatibility JavaVersion.VERSION_1_8
//        targetCompatibility JavaVersion.VERSION_1_8
    }
    lintOptions { // 编译的lint检查
        disable 'MissingTranslation' // 防止在发布的时候出现因MissingTranslation导致Build Failed!
        abortOnError false // 移除lint检查的error
    }
    packagingOptions { // 解决jar包冲突
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/DEPENDENCIES.txt'
        exclude 'META-INF/dependencies.txt'
        exclude 'META-INFTICE'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/license.txt'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/notice.txt'
        exclude 'META-INF/NOTICE.txt'
    }
    dexOptions { // 构建配置(加快编译速度)
        preDexLibraries true // 预先编译依赖库
        maxProcessCount 8 // 最大线程数
        javaMaxHeapSize "4g" // 最大堆大小
    }
    aaptOptions { // 过滤PNG图片检验
        cruncherEnabled = false
        useNewCruncher = false
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation project(':GBase')
    implementation 'com.android.support:multidex:1.0.3'
    implementation 'com.android.support:design:27.1.0'
    implementation 'com.android.support:recyclerview-v7:27.1.0'
    implementation 'com.android.support:cardview-v7:27.1.0'
    implementation 'com.android.support:appcompat-v7:27.1.0'
    implementation 'com.android.support:support-v4:27.1.0'
    implementation 'com.android.support:support-vector-drawable:27.1.0'
    implementation 'com.android.support.constraint:constraint-layout:1.0.2'
    implementation 'com.jakewharton:butterknife:8.5.1'
    annotationProcessor 'com.jakewharton:butterknife-compiler:8.5.1'
    implementation 'io.reactivex:rxjava:1.2.0'
    implementation 'io.reactivex:rxandroid:1.2.1'
    implementation 'com.squareup.retrofit2:retrofit:2.2.0'
    implementation 'com.squareup.retrofit2:adapter-rxjava:2.2.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.2.0'
    implementation 'com.squareup.retrofit2:converter-scalars:2.2.0'
    implementation 'com.squareup.okhttp3:logging-interceptor:3.7.0'
    implementation 'top.zibin:Luban:1.0.9'
    implementation 'com.google.code.gson:gson:2.8.0'
    implementation 'com.facebook.fresco:fresco:1.8.1'
    implementation 'com.facebook.fresco:animated-gif:0.13.0'
    implementation 'com.github.CymChad:BaseRecyclerViewAdapterHelper:2.9.14'
    // UI
    implementation 'com.afollestad.material-dialogs:core:0.9.6.0'
    // 三方
    implementation 'com.aliyun.dpa:oss-android-sdk:2.5.0'

}
